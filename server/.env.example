# Optional server-side keys (client can also send keys per request)
PORT=3921
SBCC_CORS_ORIGIN=*

# Default keys if not sent from client settings
PERPLEXITY_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
