# Local filled-in instance (may contain business PII — keep off public repo)
Pombomb Media/

# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Dev test deps (optional local playwright install)
node_modules/
package-lock.json

# AI server (optional local install)
server/node_modules/
