# Local filled-in instance (may contain business PII — keep off public repo)
Pombomb Media/

# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Dev test deps (optional — npm install playwright in scripts/)
node_modules/
package-lock.json
package.json
